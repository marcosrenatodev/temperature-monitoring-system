apiVersion: apps/v1
kind: Deployment
metadata:
  name: sensor-service
  namespace: temperature-monitoring
  labels:
    app: sensor-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sensor-service
  template:
    metadata:
      labels:
        app: sensor-service
    spec:
      initContainers:
      - name: wait-for-rabbitmq
        image: busybox:1.36
        command:
        - sh
        - -c
        - |
          until nc -z rabbitmq 5672; do
            echo "Waiting for RabbitMQ...";
            sleep 2;
          done;
          echo "RabbitMQ is ready!";
      - name: wait-for-api
        image: busybox:1.36
        command:
        - sh
        - -c
        - |
          until nc -z api-principal 3000; do
            echo "Waiting for API Principal...";
            sleep 2;
          done;
          echo "API Principal is ready!";
      containers:
      - name: sensor-service
        image: sensor-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3001
          name: http
        envFrom:
        - configMapRef:
            name: app-config
        - secretRef:
            name: app-secrets
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "250m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3001
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 3001
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001

---
